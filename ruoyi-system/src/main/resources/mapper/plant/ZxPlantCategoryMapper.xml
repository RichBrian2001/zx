<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.ZxPlantCategoryMapper">

    <resultMap type="ZxPlantCategory" id="ZxPlantCategoryResult">
        <id property="categoryId" column="category_id" />
        <result property="parentId" column="parent_id" />
        <result property="ancestors" column="ancestors" />
        <result property="categoryName" column="category_name" />
        <result property="categoryCode" column="category_code" />
        <result property="orderNum" column="order_num" />
        <result property="status" column="status" />
        <result property="delFlag" column="del_flag" />
        <result property="parentName" column="parent_name" />
        <result property="createBy" column="create_by" />
        <result property="createTime" column="create_time" />
        <result property="updateBy" column="update_by" />
        <result property="updateTime" column="update_time" />
        <result property="remark" column="remark" />
    </resultMap>

    <sql id="selectZxPlantCategoryVo">
        select c.category_id, c.parent_id, c.ancestors, c.category_name, c.category_code, c.order_num,
               c.status, c.del_flag, c.create_by, c.create_time, c.update_by, c.update_time, c.remark,
               (select category_name from zx_plant_category where category_id = c.parent_id and del_flag = '0') as parent_name
        from zx_plant_category c
    </sql>

    <select id="selectZxPlantCategoryList" parameterType="ZxPlantCategory" resultMap="ZxPlantCategoryResult">
        <include refid="selectZxPlantCategoryVo" />
        where c.del_flag = '0'
        <if test="categoryId != null and categoryId != 0">
            and c.category_id = #{categoryId}
        </if>
        <if test="parentId != null and parentId != 0">
            and c.parent_id = #{parentId}
        </if>
        <if test="categoryName != null and categoryName != ''">
            and c.category_name like concat('%', #{categoryName}, '%')
        </if>
        <if test="categoryCode != null and categoryCode != ''">
            and c.category_code like concat('%', #{categoryCode}, '%')
        </if>
        <if test="status != null and status != ''">
            and c.status = #{status}
        </if>
        order by c.parent_id, c.order_num, c.category_id
    </select>

    <select id="selectZxPlantCategoryById" parameterType="Long" resultMap="ZxPlantCategoryResult">
        <include refid="selectZxPlantCategoryVo" />
        where c.category_id = #{categoryId}
    </select>

    <select id="selectChildrenZxPlantCategoryById" parameterType="Long" resultMap="ZxPlantCategoryResult">
        <include refid="selectZxPlantCategoryVo" />
        where find_in_set(#{categoryId}, c.ancestors) and c.del_flag = '0'
        order by c.parent_id, c.order_num, c.category_id
    </select>

    <select id="selectNormalChildrenZxPlantCategoryById" parameterType="Long" resultType="int">
        select count(1)
        from zx_plant_category
        where del_flag = '0' and status = '0' and find_in_set(#{categoryId}, ancestors)
    </select>

    <select id="hasChildByCategoryId" parameterType="Long" resultType="int">
        select count(1)
        from zx_plant_category
        where del_flag = '0' and parent_id = #{categoryId}
        limit 1
    </select>

    <select id="checkCategoryExistPlant" parameterType="Long" resultType="int">
        select count(1)
        from zx_plant
        where del_flag = '0' and category_id = #{categoryId}
    </select>

    <select id="checkCategoryNameUnique" resultMap="ZxPlantCategoryResult">
        <include refid="selectZxPlantCategoryVo" />
        where c.del_flag = '0' and c.category_name = #{categoryName} and c.parent_id = #{parentId}
        limit 1
    </select>

    <insert id="insertZxPlantCategory" parameterType="ZxPlantCategory" useGeneratedKeys="true" keyProperty="categoryId">
        insert into zx_plant_category
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="categoryId != null and categoryId != 0">category_id,</if>
            parent_id,
            ancestors,
            category_name,
            <if test="categoryCode != null">category_code,</if>
            order_num,
            status,
            <if test="remark != null">remark,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            create_time
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="categoryId != null and categoryId != 0">#{categoryId},</if>
            #{parentId},
            #{ancestors},
            #{categoryName},
            <if test="categoryCode != null">#{categoryCode},</if>
            #{orderNum},
            #{status},
            <if test="remark != null">#{remark},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            sysdate()
        </trim>
    </insert>

    <update id="updateZxPlantCategory" parameterType="ZxPlantCategory">
        update zx_plant_category
        <set>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="ancestors != null">ancestors = #{ancestors},</if>
            <if test="categoryName != null and categoryName != ''">category_name = #{categoryName},</if>
            <if test="categoryCode != null">category_code = #{categoryCode},</if>
            <if test="orderNum != null">order_num = #{orderNum},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where category_id = #{categoryId}
    </update>

    <update id="updateZxPlantCategoryChildren" parameterType="java.util.List">
        update zx_plant_category set ancestors =
        <foreach collection="categories" item="item" separator=" " open="case category_id" close="end">
            when #{item.categoryId} then #{item.ancestors}
        </foreach>
        where category_id in
        <foreach collection="categories" item="item" separator="," open="(" close=")">
            #{item.categoryId}
        </foreach>
    </update>

    <update id="updateCategoryStatusNormal" parameterType="Long">
        update zx_plant_category set status = '0' where category_id in
        <foreach collection="array" item="categoryId" open="(" separator="," close=")">
            #{categoryId}
        </foreach>
    </update>

    <delete id="deleteZxPlantCategoryById" parameterType="Long">
        update zx_plant_category set del_flag = '2' where category_id = #{categoryId}
    </delete>

</mapper>