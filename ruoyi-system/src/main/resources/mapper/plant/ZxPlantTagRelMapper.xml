<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.ZxPlantTagRelMapper">

    <resultMap type="ZxPlantTagRel" id="ZxPlantTagRelResult">
        <id property="relId" column="rel_id" />
        <result property="plantId" column="plant_id" />
        <result property="tagId" column="tag_id" />
        <result property="tagCode" column="tag_code" />
        <result property="tagName" column="tag_name" />
        <result property="tagGroup" column="tag_group" />
        <result property="tagDesc" column="tag_desc" />
        <result property="status" column="status" />
        <result property="scoreWeight" column="score_weight" />
        <result property="createBy" column="create_by" />
        <result property="createTime" column="create_time" />
    </resultMap>

    <select id="selectSelectedTagListByPlantId" parameterType="Long" resultMap="ZxPlantTagRelResult">
        select r.rel_id, r.plant_id, r.tag_id, r.tag_code, r.score_weight, r.create_by, r.create_time,
               t.tag_name, t.tag_group, t.tag_desc, t.status
        from zx_plant_tag_rel r
        left join zx_plant_tag t on r.tag_id = t.tag_id
        where r.plant_id = #{plantId}
        order by t.sort_num, r.rel_id
    </select>

    <select id="selectAvailableTagList" resultMap="ZxPlantTagRelResult">
        select null as rel_id, null as plant_id, t.tag_id, t.tag_code, t.tag_name, t.tag_group, t.tag_desc,
               t.status, 0 as score_weight, null as create_by, null as create_time
        from zx_plant_tag t
        where t.del_flag = '0'
        order by t.sort_num, t.tag_id
    </select>

    <delete id="deleteZxPlantTagRelByPlantId" parameterType="Long">
        delete from zx_plant_tag_rel where plant_id = #{plantId}
    </delete>

    <insert id="batchInsertZxPlantTagRel" parameterType="java.util.List">
        insert into zx_plant_tag_rel (plant_id, tag_id, tag_code, score_weight, create_by, create_time)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.plantId}, #{item.tagId}, #{item.tagCode}, #{item.scoreWeight}, #{item.createBy}, sysdate())
        </foreach>
    </insert>

</mapper>